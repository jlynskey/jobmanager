<%= form_for(@jobdashboard) do |f| %>
  <% if @jobdashboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jobdashboard.errors.count, "error") %> prohibited this jobdashboard from being saved:</h2>

      <ul>
      <% @jobdashboard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job_id %><br>
    <%= f.number_field :job_id %>
  </div>
  <div class="field">
    <%= f.label :job_comment %><br>
    <%= f.text_field :job_comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
